version: "3.9"

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: crunchtime
      POSTGRES_PASSWORD: crunchtime
      POSTGRES_DB: crunchtime
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres_data: